# Ignorer les dossiers de dépendances Node
node_modules/
# Les lignes ci-dessus suffisent pour tous les dossiers node_modules dans le projet.

# Ignorer les fichiers d'environnement (contiennent des secrets et sont spécifiques à l'environnement local)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ignorer les dossiers de build/compilation
build/
dist/
frontend/build/ # Spécifique au frontend si son dossier de build est nommé 'build'
frontend/.next/ # Pour les projets Next.js (si applicable)
.parcel-cache/ # Pour les projets Parcel (si applicable)

# Ignorer les logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorer les fichiers de cache et de métadonnées spécifiques aux OS/IDE
.DS_Store
Thumbs.db
.vscode/ # Configuration VS Code spécifique à l'utilisateur
.idea/   # Configuration JetBrains IDEs (IntelliJ, WebStorm, etc.)
*.sublime-project
*.sublime-workspace
*.swp # Fichiers temporaires de Vim/Swap

# Ignorer les fichiers de couverture de test
coverage/

# Ignorer les fichiers de base de données locaux (si vous en avez)
*.sqlite
*.sqlite3

# Attention : Ignorer les fichiers PDF générés localement.
# Si vous souhaitez que les PDFs générés soient versionnés ou inclus dans le dépôt,
# supprimez la ligne "*.pdf" ci-dessous. Généralement, les fichiers générés ne sont pas versionnés.
*.pdf